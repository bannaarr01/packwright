package audit

// SlashCommand is the canonical palette label for the audit panel. It
// mirrors the ai.SlashCommand pattern so the TUI dispatch can branch
// on a single typed constant instead of a magic string.
const SlashCommand = "/audit"
